apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-cpu.fullname" . }}-test-connection"
  labels:
    {{- include "ai-cpu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-cpu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
